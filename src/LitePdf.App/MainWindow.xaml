<Window x:Class="LitePdf.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="LitePDF" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        ThemeMode="System"
        AllowDrop="True"
        Background="#F5F5F5">
    <Window.InputBindings>
        <KeyBinding Key="O" Modifiers="Ctrl" Command="{Binding OpenCommand}"/>
        <KeyBinding Key="OemPlus" Modifiers="Ctrl" Command="{Binding ZoomInCommand}"/>
        <KeyBinding Key="Add" Modifiers="Ctrl" Command="{Binding ZoomInCommand}"/>
        <KeyBinding Key="OemMinus" Modifiers="Ctrl" Command="{Binding ZoomOutCommand}"/>
        <KeyBinding Key="Subtract" Modifiers="Ctrl" Command="{Binding ZoomOutCommand}"/>
        <KeyBinding Key="D0" Modifiers="Ctrl" Command="{Binding ZoomResetCommand}"/>
        <KeyBinding Key="D1" Modifiers="Ctrl" Command="{Binding FitWidthCommand}"/>
        <KeyBinding Key="D2" Modifiers="Ctrl" Command="{Binding FitPageCommand}"/>
        <KeyBinding Key="G" Modifiers="Ctrl" Command="{Binding GoToPageCommand}"/>
        <KeyBinding Key="F4" Command="{Binding ToggleSidebarCommand}"/>
        <KeyBinding Key="F" Modifiers="Ctrl" Command="{Binding SearchCommand}"/>
        <KeyBinding Key="Home" Command="{Binding FirstPageCommand}"/>
        <KeyBinding Key="End" Command="{Binding LastPageCommand}"/>
    </Window.InputBindings>

    <DockPanel>
        <!-- Toolbar -->
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Content="Open" Command="{Binding OpenCommand}" ToolTip="Open (Ctrl+O)"/>
                <Separator/>
                <Button Content="☰" Command="{Binding ToggleSidebarCommand}" ToolTip="Toggle sidebar (F4)"/>
                <Separator/>
                <Button Content="◀" Command="{Binding PrevPageCommand}" ToolTip="Previous page"/>
                <TextBox x:Name="PageBox" Width="50" Text="{Binding CurrentPageNumber, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" Margin="2,0" PreviewKeyDown="PageBox_PreviewKeyDown"/>
                <TextBlock Text="{Binding PageCount, StringFormat=/ {0}}" VerticalAlignment="Center" Margin="2,0"/>
                <Button Content="▶" Command="{Binding NextPageCommand}" ToolTip="Next page" Margin="2,0,0,0"/>
                <Separator/>
                <Button Content="−" Command="{Binding ZoomOutCommand}" ToolTip="Zoom out (Ctrl+-)"/>
                <ComboBox x:Name="ZoomBox" Width="80" IsEditable="True" Text="{Binding ZoomPercent, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" Margin="2,0" SelectionChanged="ZoomBox_SelectionChanged" PreviewKeyDown="ZoomBox_PreviewKeyDown">
                    <ComboBoxItem Content="25%"/>
                    <ComboBoxItem Content="50%"/>
                    <ComboBoxItem Content="75%"/>
                    <ComboBoxItem Content="100%"/>
                    <ComboBoxItem Content="125%"/>
                    <ComboBoxItem Content="150%"/>
                    <ComboBoxItem Content="200%"/>
                    <ComboBoxItem Content="400%"/>
                </ComboBox>
                <Button Content="+" Command="{Binding ZoomInCommand}" ToolTip="Zoom in (Ctrl+=)"/>
                <Button Content="Fit W" Command="{Binding FitWidthCommand}" ToolTip="Fit width (Ctrl+1)" Margin="4,0,0,0"/>
                <Button Content="Fit P" Command="{Binding FitPageCommand}" ToolTip="Fit page (Ctrl+2)"/>
                <Separator/>
                <Button Content="Copy text" Command="{Binding CopyPageTextCommand}" ToolTip="Copy page text"/>
                <Button Content="OCR page" Command="{Binding OcrPageCommand}" ToolTip="OCR current page"/>
            </ToolBar>
        </ToolBarTray>

        <!-- Search bar -->
        <Border x:Name="SearchBar" DockPanel.Dock="Top" Background="#E0E0E0" Padding="8,4" Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox x:Name="SearchBox" Grid.Column="1" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,0,8,0" PreviewKeyDown="SearchBox_PreviewKeyDown"/>
                <Button Grid.Column="2" Content="Find" Command="{Binding SearchCommand}" Margin="0,0,4,0" Padding="8,2"/>
                <Button Grid.Column="3" Content="▲" Command="{Binding SearchPrevCommand}" ToolTip="Previous" Margin="0,0,2,0" Padding="6,2"/>
                <Button Grid.Column="4" Content="▼" Command="{Binding SearchNextCommand}" ToolTip="Next" Margin="0,0,8,0" Padding="6,2"/>
                <Button Grid.Column="5" Content="✕" Command="{Binding ClearSearchCommand}" Click="CloseSearch_Click" Padding="6,2"/>
            </Grid>
        </Border>

        <!-- Status bar -->
        <StatusBar DockPanel.Dock="Bottom">
            <TextBlock Text="{Binding FileName}" Margin="4,0"/>
            <Separator/>
            <TextBlock Text="{Binding CurrentPageNumber, StringFormat=Page {0}}" Margin="4,0"/>
            <TextBlock Text="{Binding PageCount, StringFormat=of {0}}" Margin="0,0,4,0"/>
            <Separator/>
            <TextBlock Text="{Binding ZoomPercent}" Margin="4,0"/>
            <Separator/>
            <TextBlock Text="{Binding StatusText}" Margin="4,0"/>
        </StatusBar>

        <!-- Main content -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="SidebarColumn" Width="280"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Grid x:Name="Sidebar" Grid.Column="0" Background="#FAFAFA">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TabControl Grid.Row="1" Margin="2">
                    <TabItem Header="Pages">
                        <ListBox x:Name="ThumbnailList" ItemsSource="{Binding Thumbnails}" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True" SelectionMode="Single" Background="Transparent" BorderThickness="0" SelectionChanged="ThumbnailList_SelectionChanged">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderThickness="2" Margin="4" Background="White" Width="140">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="BorderBrush" Value="DodgerBlue"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid>
                                            <Image Source="{Binding Bitmap}" Stretch="Uniform" MinHeight="80"/>
                                            <TextBlock Text="{Binding Index, StringFormat=Page {0}}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Background="#AAFFFFFF" Padding="2" FontSize="10"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                        </ListBox>
                    </TabItem>
                    <TabItem Header="Chapters">
                        <Grid>
                            <TreeView x:Name="OutlineTree" ItemsSource="{Binding Outline}" Background="Transparent" BorderThickness="0" SelectedItemChanged="OutlineTree_SelectedItemChanged">
                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                        <TextBlock Text="{Binding Title}" ToolTip="{Binding Title}" TextTrimming="CharacterEllipsis" MaxWidth="200">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                            <Setter Property="FontWeight" Value="Bold"/>
                                                            <Setter Property="Foreground" Value="DodgerBlue"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                            <TextBlock x:Name="NoChaptersText" Text="No chapters in this document" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.6" Visibility="Collapsed"/>
                        </Grid>
                    </TabItem>
                    <TabItem Header="Search">
                        <ListBox ItemsSource="{Binding SearchResults}" SelectionChanged="SearchResults_SelectionChanged" Background="Transparent" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="4">
                                        <TextBlock Text="{Binding PageIndex, StringFormat=Page {0}}" FontWeight="Bold" FontSize="10"/>
                                        <TextBlock Text="{Binding Snippet}" TextWrapping="Wrap" FontSize="12"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </TabItem>
                    <TabItem Header="Annotations">
                        <ListBox ItemsSource="{Binding Annotations}" Background="Transparent" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="4">
                                        <TextBlock Text="{Binding PageIndex, StringFormat=Page {0}}" FontWeight="Bold" FontSize="10"/>
                                        <TextBlock Text="{Binding Contents}" TextWrapping="Wrap" FontSize="12"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </TabItem>
                </TabControl>
            </Grid>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" Background="#DDD"/>

            <!-- Document view -->
            <ScrollViewer x:Name="DocumentScrollViewer" Grid.Column="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Background="#EEE" PreviewMouseWheel="DocumentScrollViewer_PreviewMouseWheel" ScrollChanged="DocumentScrollViewer_ScrollChanged">
                <ItemsControl x:Name="PagesControl" ItemsSource="{Binding Pages}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel IsVirtualizing="True" VirtualizationMode="Recycling" ScrollUnit="Pixel"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" Margin="12" Width="{Binding DipWidth}" Height="{Binding DipHeight}" HorizontalAlignment="Center" VerticalAlignment="Top">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                                </Border.Effect>
                                <Grid>
                                    <Image x:Name="PageImage" Source="{Binding Bitmap}" Stretch="Fill" RenderOptions.BitmapScalingMode="HighQuality"/>
                                    <!-- Selection overlay would go here -->
                                    <Border x:Name="LoadingOverlay" Background="#F0F0F0" Visibility="Collapsed">
                                        <TextBlock Text="Loading..." HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
                                    </Border>
                                </Grid>
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsRendering}" Value="True">
                                    <Setter TargetName="LoadingOverlay" Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </DockPanel>
</Window>
