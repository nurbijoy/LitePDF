<UserControl x:Class="LitePdf.App.Views.PageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="600">
    <Border Background="White" Margin="12" HorizontalAlignment="Center" VerticalAlignment="Top" x:Name="RootBorder">
        <Border.Effect>
            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
        </Border.Effect>
        <Grid x:Name="PageGrid" Background="White">
            <Image x:Name="PageImage" Stretch="Fill" RenderOptions.BitmapScalingMode="HighQuality"/>
            <!-- Selection rectangles -->
            <Canvas x:Name="SelectionCanvas" IsHitTestVisible="False"/>
            <!-- Search highlight -->
            <Canvas x:Name="SearchCanvas" IsHitTestVisible="False"/>
            <!-- Annotation quads -->
            <Canvas x:Name="AnnotationCanvas" IsHitTestVisible="False"/>
            <!-- Links overlay -->
            <Canvas x:Name="LinkCanvas" Background="Transparent"/>
            <!-- Loading overlay -->
            <Border x:Name="LoadingOverlay" Background="#F0F0F0" Visibility="Collapsed">
                <TextBlock Text="Loading..." HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
            </Border>
            <!-- Scanned banner -->
            <Border x:Name="ScannedBanner" Background="#FFF9C4" BorderBrush="#FBC02D" BorderThickness="1" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="4" Padding="6" Visibility="Collapsed">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="This page has no text." VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <Button Content="Run OCR" Click="RunOcr_Click" Padding="6,2" Margin="0,0,4,0"/>
                    <Button Content="OCR all pages" Click="OcrAll_Click" Padding="6,2"/>
                    <Button Content="✕" Click="CloseBanner_Click" HorizontalAlignment="Right" Margin="8,0,0,0" Padding="4,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
